GM_SEGA:
		MOVE.B	#$E4,	D0
		BSR.W	PLAYSOUND_SPECIAL
		BSR.W	CLEARPLC
		BSR.W	PALETTEFADEOUT
		LEA		(VDP_CONTROL_PORT).L,	A6
		MOVE.W	#$8004,	(A6)
		MOVE.W	#$8230,	(A6)
		MOVE.W	#$8407,	(A6)
		MOVE.W	#$8700,	(A6)
		MOVE.W	#$8B00,	(A6)
		CLR.B	(F_WTR_STATE).W
		MOVE	#$2700,	SR
		MOVE.W	(V_VDP_BUFFER1).W,	D0
		ANDI.B	#$BF,	D0
		MOVE.W	D0,	(VDP_CONTROL_PORT).L
		BSR.W	CLEARSCREEN
		MOVE.W	(V_VDP_BUFFER1).W,	D0
		ORI.B	#$40,	D0
		MOVE.W	D0,	(VDP_CONTROL_PORT).L
		MOVE	#$2300,	SR
		
	SEGA_WAITEND:
		MOVE.B	#2,	(V_VBLA_ROUTINE).W
		BSR.W	WAITFORVBLA
		CMPI.B	#0,	(V_GAMEMODE).W
		BEQ.S	SEGA_WAITEND
		RTS